name: Print Workflow
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Hello, World!"